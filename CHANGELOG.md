# Beans Music 更新日志

## 1.14.0

### 修复（网易云歌曲的播放等待与自动切歌）
- **网易云的歌不用再等半天了**：这些歌的播放地址原先是一档接一档地试 —— pyncmd（超时 4 秒）→ 官方接口（8 秒）→ 官方降档（再 8 秒）→ 第三方音源（7 秒），最坏要二十多秒才轮到出声。现在三档同时上路，谁先给出可用地址就立刻播。插件音源（B 站等）那条分支在 1.13.0 已经改成了并发，这次补上的是网易云这条主路径 —— 这也正是「本地歌单里的 B 站歌很顺、歌单里的网易云歌要等好久」这个差异的来源。
- **第三方音源档延后 1.5 秒才发起**：前两档能命中时它会被取消（取消发生在等待阶段，一个网络请求都不会发出去）；前两档都不行时它已经提前上路，不必让用户再从零等一遍。
- **修复「有时候会莫名其妙跳过一首」**：`AVPlayerItemDidPlayToEndTime` 通知是投递到主队列的，如果「上一首自然播完」的消息刚入队、用户这一瞬间已经手动切了歌，这条迟到的通知会再把队列往前推一首。现在补上了「确认这条通知说的就是当前这首」的校验（失败回调早就有同样的校验）。
- **pyncmd 地址缓存 60 秒 → 8 分钟**：直链本身有效期约 20 分钟，原先的 60 秒短到「上一首播完自动切下一首」时缓存必然已过期，每首歌都要重新请求一遍。
- **新增预取下一首**：一首歌播到 70% 时（且时长 > 60 秒），后台先把下一首的直链取回来预热缓存。自动切歌那一刻是内存命中，接近零等待。

## 1.13.0

### 修复（播放与流畅度）
- **「新歌还在找音源时按播放键，结果播回上一首」—— 修的是真正的入口**：上一版是在「解析彻底失败后」才释放播放器，但解析本身是异步的（可能几秒到十几秒），这段时间旧 AVPlayer 一直停在上一首的 item 上，按播放键就会命中它。现在切歌那一刻（`loadCurrent` 入口）就把旧播放器作废，播放键的语义稳定地回到「重试当前这一首」。
- **解析音源期间按播放键**：改为提示「正在寻找可用音源…」。此前会重新触发一次 `loadCurrent`，等待从头计时 —— 越按越慢。
- **插件歌曲取址改为并发**：「匹配网易云 + pyncmd 换链」与「插件自身解析」两条路径同时发起，谁先给出可用地址就用谁。原先是串行，而两档各有自己的网络超时（搜索接口 20 秒、插件 10–12 秒），最坏情况是两者**相加**。
- **单档硬预算**：网易云搜索 4 秒、官方取址 8 秒。这一档处在顺位链最前面，接口不响应时会把后面所有档位一起拖住。
- **底栏滑动不再发涩**：`KumoneGlassTabBar` 的拖拽手势原先「滑过哪一格就切哪一页」，手指从「主页」划到「我的」会连续触发三次 `selection` 变更、连续构建三个 Tab 页，每层还套着 spring 动画。现在拖动只移动指示胶囊，松手才切一次。
- **Tab 切换去掉隐式动画**：外层那条 `.animation(…, value: selection)` 会让新页面里所有可动画属性跟着做一遍过渡，而切换那一帧正是新页面首次构建的峰值。

## 1.12.0

### 新增
- 「我的」页面不再只放一个**入口卡片**：一键同步歌单、备份、恢复和歌单列表整段直接铺在页面上，滑到就能用，不用再点进二级页面。

### 修复（流畅度）
- **去掉全站实时模糊**：卡片、按钮、底部浮岛（悬浮标签栏 + 迷你播放器）背后的 `.ultraThinMaterial` / `.regularMaterial` 换成静态玻璃面板。材质是实时背景模糊，压在滚动内容上的每一层都要逐帧重新卷积 —— 换成一次普通绘制后，滚动明显更顺，也不那么容易发热。全屏播放页自带的整屏封面模糊是另一套实现（切歌时一次生成、前台静态显示），不受影响。
- **日志写盘改到后台队列**：以前每记一条日志都在当前线程上做一遍 createDirectory + stat + open/write/close，其中不少调用来自主线程（UI 事件、播放状态变化）。现在主线程只做一次入队，同一天的日志文件 URL 也只解析一次。

## 1.11.0

### 新增
- 「我的」页面新增**本地歌单**入口：点进去就是本机歌单页，不用再去音乐库滚过整张网易云歌单。
- 「一键同步歌单」旁边新增**备份 / 恢复**：把本机歌单导出成一个文件（与 WebDAV 快照同格式），换设备导入即可回到手上；同名歌曲沿用「跳过 / 替换」策略，不会越导越多。

### 修复（播放卡住）
- **「切到一首放不出来的歌，再点播放键却退回上一首」**：解析彻底失败后把播放器及其观察者整体释放。原先 AVPlayer 还停在上一首的 item 上，点播放键会直接对旧 item 调 `playImmediately`，于是又放出上一首。释放之后，播放键的语义回到「重试当前这一首」。
- **插件音源顺位走不完**：pyncmd 与插件自身都拿不到地址时，再往下走一档通用第三方音源（按歌名搜），全部试过才判失败 —— 与设置页「顺位」文案一致。顺带修好插件歌曲在「地址失效重试」链上被直接早退、假装重试成功的问题。
- **自动下一首的等待**：由 10 秒缩到 3 秒；连续多首都放不出来时最多跳 8 首就停下并提示，不再无止境往下跳。

## 1.10.0

### 新增
- 加入本地歌单的歌曲改放到列表**最上面**（批量添加保持原有顺序），加完立刻可见，不必滚到几百首的末尾找。
- 封面按显示尺寸解码：列表只解缩略图（256 档），播放页才解大图（1280 档）；同一 URL 不同尺寸各存一份缓存。

### 修复（流畅度）
- **列表行底去掉实时毛玻璃**：原先每行一个 `.ultraThinMaterial`，一屏十几行就是十几个离屏模糊层，滚动时每帧重新合成 —— iPhone 12 上「一滚就掉帧、一热更明显」的主要来源。改为同色系半透明底。
- **背景装饰光晕**：由「实心圆 + blur(100/110)」改为等效径向渐变。GlassBackdrop 几乎每页都在用，这层模糊是整屏最贵的离屏渲染。
- **持续动画限帧**：唱片旋转、唱臂摆动、封面自转从屏幕最高刷新率（可达 120Hz）限到 30fps，播放页不再持续满载 GPU，减少发热降频。
- **锁屏封面**：改为 URLSession 异步 + 600 像素缩略图，缓存加 24 MB 上限（原先无上限、且存整张原图）。
- **图片文件缓存**：`BeansImageFileCache` 加 cost 记账与 48 MB 上限（原先无上限，壁纸/歌词背景这类整屏大图会把内存顶到几百 MB）。
- 封面缓存键做记忆化，避免列表滚动时每行每次重算 SHA256。

## 1.9.1

### 新增
- 导入设置新增「导入时遇到重复的歌曲」：跳过 / 替换。同一首歌按来源标识相同、或歌名相同且时长差 5 秒内判定；跳过让反复导入不堆重复，替换可把别的设备上换好音源的条目带回来。
- 导入云端文件时优先识别 Beans 自己导出的快照格式（app == "Beans Music"），完整还原歌手、封面与插件音源信息。

### 修复
- WebDAV 导入 Beans 快照后歌曲变成「未知歌手、无封面、无法播放」：快照此前被误当 MusicFree 备份递归解析，source 被误读成平台名，pluginItemID / pluginRawJSON 全部丢失，identityKey 改变导致去重失效、重复添加。

## 1.9.0

### 新增与优化

- 新增内置音源 **pyncmd**：按网易云歌曲 id 换取原站直链，最高档实测为无损（约 800 kbps），单次请求通常 1 秒内返回。不需要导入音源、不需要填密钥。
- 解析顺位改为 **pyncmd → 官方接口 → 你导入的第三方音源 → 插件音源本身**，任一顺位拿到可用地址就立刻播放，不会等后面的。
- **插件音源的歌曲也先试 pyncmd**：按歌名 + 歌手 + 时长三者都吻合才认，认不出（例如哔哩哔哩视频）就退回插件自己解析。这是解决「插件只给 320k、每次解析都慢」的主要办法。
- 设置页新增 **「首选音源 → 解析顺位设置」**：可单独开关 pyncmd、单独控制「网易云歌曲优先」与「插件歌曲优先」两个生效范围，并可选择 pyncmd 音质档（最高 / 320k / 128k）。
- **本地歌单支持逐曲更换音源**：歌曲右键菜单 → 「更换音源」，在网易云 / QQ / 酷狗 / 各个已装插件里按歌名并发匹配，挑一条替换即可。替换是原地的，歌单里的位置与顺序不变。
- 换源面板会给「歌手 + 时长」都对得上的候选打上对勾，避免误换成翻唱或 Live 版本；界面同时列出歌手与时长供人工判断。

## 1.8.2

### 新增与优化

- 播放页右上角新增独立的评论按钮，位置在红心左边，点一下直接进评论区，不必再先进「⋯」菜单。
- 黑胶布局、歌曲页布局、Apple Music 版播放页三套顶栏均已同步调整，样式与旁边的红心一致。

## 1.8.1

### 新增与优化

- 播放页右上角「⋯」菜单新增「查看评论」：黑胶布局与歌曲页布局都能直接进。

### 修复与调整

- 修复点「添加到歌单」里的网易云歌单后毫无反应的问题：面板现在会正常关闭，并给出成功或失败提示。
- 修复网易云歌单加歌始终没有真正写入的问题：写接口按服务端当前认的协议发出，失败时会显示网易云返回的真实原因（如歌单不属于自己、登录态失效），不再是一句笼统的「添加失败」。
- 收藏来的网易云歌单现在标明「收藏的歌单，不能往里加歌」，不再让用户点一个必然失败的项。
- 修复评论页会一直转圈不出内容的问题。

## 1.8.0

### 新增与优化

- 本机歌单支持调整歌曲顺序：歌单详情右上角菜单选「调整歌曲顺序」，拖动右侧手柄排序，顺序自动保存；搜索状态下会暂时禁止拖动，避免把歌挪到错位置。
- 「添加到歌单」合并成一个面板：本机歌单与网易云歌单同屏可选，网易云来源的歌曲可以直接写回自己的网易云歌单，也可以当场新建网易云歌单。
- 歌曲右键菜单新增「查看评论」。
- 评论按来源分流：网易云、QQ音乐、酷狗音乐各看各的评论，插件音源（哔哩哔哩）看 B 站视频评论。

### 修复与调整

- 修复插件音源歌曲的评论页会去查网易云评论、显示成不相干内容的问题。
- 网易云之外的音源不再显示无法工作的网易云歌单入口，改为一句明确提示。

## 1.7.0

### 新增与优化

- 音乐库新增「插件音源」入口，与网易云、QQ音乐、酷狗音乐并列，左上角一键切换。
- 支持 MusicFree 插件音源：哔哩哔哩等插件可直接搜歌、浏览榜单与歌单，并一键导入本地歌单。
- 本机歌单支持把网易云、QQ音乐、酷狗音乐、插件音源的歌曲混装在一起。
- 新增 WebDAV 同步：本机歌单可上传到自己的 WebDAV 网盘，换机或重装后一键恢复合并。
- 新增封面图片缓存，列表滚动与页面往返不再反复加载封面。

### 修复与调整

- 修复从播放页返回首页后，右下角迷你播放器封面反复闪烁的问题。
- 修复版本号回退导致的「发现新版本」误提示。
- 后台链路默认不再强制拉满刷新率，减少发热与降频掉帧。

## 1.6.4

### 新增与优化

- 优化本地播放、主题和播放器设置体验。

### 修复与调整

- 整理公开客户端工程结构，保持客户端功能独立。

## 1.5.8

### 新增与优化

- 新增网易云音乐、QQ音乐、酷狗音乐歌单一键同步到本地。
- 本地歌单支持编辑和搜索，批量选择歌曲后可添加到其他本地歌单。
- 新增 QQ 音乐热门歌单展示。
- 主页问候语支持自定义文字、颜色、大小、发光、专属字体、底部横线和上下渐变，并可逐行选择渐变颜色。
- 播放器支持左右滑动切换歌曲，新增顶部三平台排序、隐藏主页刷新/用户名/排序按钮。
- 播放器按钮图标样式新增，播放器设置界面重新整理分组和控件排版。
- 主页、音乐库、我的、设置页增加 iPad 最大宽度适配。
- 播放列表、最近播放和日志界面背景同步主页壁纸。

### 修复与调整

- 修复 QQ 音乐喜欢列表不显示的问题。
- 修复最近播放、日志、本地歌单和播放列表不同步主页壁纸的问题。
- 修复设置页掉帧问题；如果本次仍然掉帧，建议更换设备。
- 修复 iOS 15 编译兼容问题。

## 1.5.6

### 新增与优化

- 封面页歌名、歌手和预览歌词支持渐变、高光及高光强度调节。
- 播放页背景浮尘新增开关，默认关闭；动态浮尘支持密度和大小调节。
- 全局上传壁纸自动同步到播放器封面页背景。
- 播放页支持从顶部下划关闭，并加入缩放、淡出动画。

### 修复与调整

- 修复酷狗排行榜歌曲封面缺失或未归一化的问题。
- 提高内置音源搜索上限。
- 播放器设置打开后不再默认展开“播放”和“歌词显示”分组。
- 修复部分自签安装后启动即退出的问题：将系统媒体服务、字体注册和壁纸恢复移出 App 初始化阶段，并保留播放时的系统音频初始化兜底。

## 1.5.5

### 重要提示

> <span style="color:#ff7a00;font-weight:700;">从 1.5.4 版本开始，播放器设置已从右上角删除，改为点击中间歌曲正在播放的标题打开。</span>

### 新增与优化

- 优化播放中全局刷新策略，移除高刷保持器的常驻空转刷新，降低设置页、我的页面和播放器页面的发热与掉帧。
- 本地壁纸、歌词背景、设置页缩略图改为复用解码缓存，减少滚动和切换设置时的重复图片解码。
- 锁屏/系统正在播放封面增加缓存，避免播放状态变化时反复下载和刷新同一张封面。
- 聆澜内置音源支持多密钥池，当前密钥未命中时自动切换下一个，并记住最近可用密钥。
- 播放器设置新增封面页歌名、歌手、预览歌词与未播放歌词颜色调节。

### 修复

- 修复播放中进度更新过于频繁导致非播放器页面也跟随重绘的问题。
- 修复重新上传歌词背景或恢复壁纸后，部分位置可能继续显示旧图片缓存的问题。
- 修复酷狗排行榜详情歌曲封面链接未归一化，并在官网榜单缺封面时自动用移动端榜单数据补齐封面。
- 优化巨魔安装场景下播放中切换页面的刷新与解码负担。
